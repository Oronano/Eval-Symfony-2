{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
    <h1>add cat</h1>
    {{ form_start(form) }} 
        {{form_errors(form) }}
        {{ form_row(form.titre) }}
    {{ form_end(form) }}

    <a href="{{ path('app_categories') }}">retour</a>
{% endblock %}
