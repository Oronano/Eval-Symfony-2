{% extends 'base.html.twig' %}

{% block body %}
    <h1>add article</h1>
    {{ form_start(form) }} 
        {{form_errors(form) }}
        {{ form_row(form.titre) }}
        {{ form_row(form.etat) }}
        {{ form_row(form.auteur) }}
        {{ form_row(form.categorie) }}
    {{ form_end(form) }}

    <a href="{{ path('app_article') }}">retour</a>
{% endblock %}